name: agrotech_mobile
description: AgroTech SMAT - Aplicación Móvil

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  fl_chart: ^1.2.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.0

flutter:
  uses-material-design: true
